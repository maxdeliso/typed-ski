module ShortCircuit
import Prelude Bool
import Prelude true
import Prelude false
import Prelude and
import Prelude or
import Prelude not
import Prelude U8
export main
poly fail = \u : U8 => not (fail u)
poly main =
  let a = and false (fail #u8(0)) in
  let b = or true (fail #u8(0)) in
  true
