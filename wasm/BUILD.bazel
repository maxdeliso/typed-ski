load("//bazel:wasm_release.bzl", "wasm_release")

package(default_visibility = ["//visibility:public"])

wasm_release(
    name = "release",
    srcs = ["//core:arena.c"],
    hdrs = [
        "//core:arena.h",
        "//core:host_platform.h",
    ],
)
